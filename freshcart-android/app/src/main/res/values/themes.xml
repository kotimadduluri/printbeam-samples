<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.FreshCart" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Matches the Compose "ground" token so there's no white flash before setContent. -->
        <item name="android:windowBackground">#F4F6F3</item>
    </style>
</resources>
